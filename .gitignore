.venv/
.env
__pycache__/
*.pyc
# Auto-created by the Streamlit dev skill; symlinks into .venv
.agents/
.claude/skills/developing-with-streamlit
# data/jobs.sqlite3 is intentionally tracked - GitHub Actions commits it back
# after each scan so first_seen/notified_at persist across scheduled runs.
